#include <stdio.h>
int main()
{
    int a, b, c, d, e, p;
    printf("enter the score of five subjects :");
    scanf("%d%d%d%d%d", &a, &b, &c, &d, &e);

    p = (a + b + c + d + e) / 5;
    

    return 0;
}